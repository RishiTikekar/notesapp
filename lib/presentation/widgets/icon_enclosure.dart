import 'package:flutter/material.dart';

class IconEnclosure extends StatelessWidget {
  const IconEnclosure({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
